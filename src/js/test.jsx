import React from 'react';

export default function Test(props) {
    return (
        <div>
            <h1>Hello, World!</h1>
            <p>Now is {new Date().toString()}</p>
        </div>
    );
}